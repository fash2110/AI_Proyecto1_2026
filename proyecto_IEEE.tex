\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{url}
\usepackage{hyperref}
\usepackage{booktabs} % Para mejores tablas
% TeX Live ``basic'' a veces no incluye Courier (pcr); usar cmtt para \texttt
\renewcommand{\ttdefault}{cmtt}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}

\title{Proyecto I: Reconocimiento de comandos de voz y
despliegue móvil con ONNX Runtime Mobile}

\author{\IEEEauthorblockN{Fabricio Mena Mejía}
\IEEEauthorblockA{Instituto Tecnológico de Costa Rica\\
Email: fabriciomena11@estudiantec.cr}
\and
\IEEEauthorblockN{Anthony Fuentes Calvo}
\IEEEauthorblockA{Instituto Tecnológico de Costa Rica\\
Email: anthonyfuentescalvo@estudiantec.cr}
\and
\IEEEauthorblockN{Fernando Sánchez Hidalgo}
\IEEEauthorblockA{Instituto Tecnológico de Costa Rica\\
Email: Fer.sanchez@estudiantec.cr}}

\maketitle

\begin{abstract}
Este informe corresponde al Proyecto~I del curso de Inteligencia Artificial: se construye, de extremo a extremo, un reconocedor de comandos de voz de vocabulario reducido orientado a un escenario de asistencia o control (p.\,ej.\ hogar inteligente), pasando del dato crudo al despliegue en dispositivo. Siguiendo el enunciado, se adopta el conjunto público \textit{Google Speech Commands} v0.02 con diez clases, partición oficial de entrenamiento, validación y prueba, y una representación tipo imagen mediante mel-espectrograma normalizado y reescalado a $128 \times 128$ para alimentar redes convolucionales en PyTorch. Se comparan dos familias de modelos---LeNet-5 adaptado (con \texttt{Tanh}) y MobileNetV2 en una variante ligera---bajo datos crudos frente a datos aumentados (desplazamiento temporal, ruido y SpecAugment) y tres configuraciones de hiperparámetros por combinación, lo que produce doce entrenamientos comparables. Las métricas en prueba muestran superioridad sostenida de MobileNetV2 (hasta $0.9633$ de exactitud en la mejor corrida). El seguimiento experimental se centralizó en Weights \& Biases. Por último, se documenta la línea de trabajo hacia la exportación ONNX, la comprobación de paridad con respecto a PyTorch y la integración prevista con ONNX Runtime Mobile en una aplicación Android (Jetpack Compose) que mapea la predicción a acciones de interfaz.
\end{abstract}

\begin{IEEEkeywords}
Comandos de voz, mel-espectrograma, redes neuronales convolucionales, aumentación de datos, Weights \& Biases, ONNX, ONNX Runtime Mobile.
\end{IEEEkeywords}

\section{Marco teórico}

La clasificación de audio corto puede formularse como reconocimiento de patrones en una imagen tiempo-frecuencia: a partir de una forma de onda muestreada se calcula una transformada de Fourier de tiempo corto (STFT) y un banco de filtros mel que aproxima la percepción humana de tono; el logaritmo de la energía mel amortigua la dinámica y facilita el entrenamiento de redes profundas \cite{torchaudio_docs}. Las redes neuronales convolucionales (CNN) en dos dimensiones extraen jerarquías locales de texturas y formas en ese mapa, de forma análoga a la visión por computador aplicada a representaciones espectrales.

Para mejorar la generalización bajo ruido y variaciones de canal, es habitual aplicar aumentación en el dominio del tiempo (p.\,ej.\ desplazamiento) y en el espectro; SpecAugment enmascara franjas de tiempo y frecuencia en el mel-espectrograma durante el entrenamiento, obligando al modelo a no depender de un único bin o frame \cite{specaugment}.

En dispositivos móviles, conviene separar entrenamiento (PyTorch) e inferencia ligera: exportar el grafo a ONNX en modo evaluación, con pesos congelados, y ejecutarlo con ONNX Runtime Mobile reduce dependencias y habilita optimizaciones de tiempo de ejecución \cite{onnxruntime}. La calidad del despliegue exige que el preprocesamiento en el terminal replique fielmente el usado al entrenar; la discrepancia se controla con pruebas de paridad logit-a-logit entre PyTorch y el runtime ONNX \cite{onnxruntime}.

\section{Dataset y preprocesamiento}

\subsection{Speech Commands v0.02 y subconjunto de diez clases}
Se utilizó el conjunto \textit{Speech Commands} v0.02 distribuido por TensorFlow Datasets \cite{tensorflow_datasets_speech_commands}, compuesto por grabaciones en formato WAV de un segundo o menos, organizadas en carpetas por etiqueta léxica. Siguiendo el alcance del proyecto, se trabajó con diez comandos: \texttt{yes}, \texttt{no}, \texttt{up}, \texttt{down}, \texttt{left}, \texttt{right}, \texttt{on}, \texttt{off}, \texttt{stop} y \texttt{go}.

\subsection{División entrenamiento, validación y prueba}
La partición oficial del corpus se respeta mediante los archivos \texttt{validation\_list.txt} y \texttt{testing\_list.txt} incluidos en la raíz del dataset: las rutas listadas en ellos definen los conjuntos de validación y de prueba, respectivamente. El entrenamiento comprende el resto de archivos \texttt{.wav} de las diez clases que no aparecen en dichas listas, lo que garantiza que no haya solapamiento de hablantes ni clips entre particiones y que los resultados sean comparables con la literatura.

\subsection{Representación tipo imagen y flujo reproducible}
Cada clip se re-muestrea a mono PCM de 16\,kHz y se recorta o rellena a una ventana fija de 1\,s (16\,000 muestras) antes del análisis espectral. Para asegurar consistencia entre entrenamiento y despliegue m\'ovil, se fija un \textbf{\'unico contrato de preprocesamiento} (usado para exportaci\'on ONNX y para inferencia en Android): STFT con ventana Hann de 25\,ms (\texttt{win\_length}=400), \texttt{n\_fft}=512 y \texttt{hop\_length}=160 (10\,ms); banco mel HTK de \texttt{n\_mels}=64 entre 20--8000\,Hz; y mel-espectrograma en escala logar\'{\i}tmica \(\log(\text{mel}+\varepsilon)\). El mapa \((64\times101)\) resultante se re-escala bilinealmente a \(128\times128\) y se normaliza para formar el tensor de entrada en NCHW \texttt{(1,1,128,128)}, compatible con las entradas descritas en las Tablas~\ref{tab:arquitectura} y~\ref{tab:arquitectura_b}. Este contrato se implementa de manera equivalente en el cuaderno de entrenamiento (\texttt{proy1.ipynb}) y en la aplicaci\'on m\'ovil, evitando discrepancias silenciosas entre PyTorch y ONNX Runtime Mobile.

La reproducibilidad se apoya en la semilla global 42 (v\'ease el control de aleatoriedad en la siguiente secci\'on), en el uso determinista de las listas oficiales de validaci\'on y prueba y en la trazabilidad de hiperpar\'ametros y curvas en Weights \& Biases.

\section{Diseño de arquitectura de IA}

\subsection{Control de Aleatoriedad y Reproducibilidad}
Para garantizar la reproducibilidad de los experimentos, se implementó un estricto control de aleatoriedad. Se definió una semilla global (\textit{seed} $= 42$) utilizada para inicializar los generadores de números pseudoaleatorios de las librerías \texttt{random}, \texttt{numpy} y \texttt{torch}. Adicionalmente, se configuró el comportamiento determinista de cuDNN desactivando el \textit{benchmark} dinámico donde fuera necesario.

\subsection{Selección de Técnicas de Aumentación de Datos}
Para mejorar la capacidad de generalización de los modelos y simular condiciones realistas de entorno (ruido de fondo, variabilidad de hablantes), se aplicaron técnicas de aumento de datos (\textit{data augmentation}) en el preprocesamiento de audio. Las técnicas se inspiraron en métodos avanzados de aumento espectral y en el dominio del tiempo:
\begin{itemize}
    \item \textbf{Time Shift:} Desplazamiento temporal aleatorio para que el sistema aprenda predicciones invariantes al milisegundo exacto de reproducción, alineándose directamente con aplicaciones reales bajo ruidos dispares.
    \item \textbf{Ruido Blanco (White Noise):} Inyectado progresivamente para simular grabaciones en ambientes no controlados y servir de fuerte regularización matemática de los tensores.
    \item \textbf{SpecAugment \cite{specaugment}:} Enmascaramiento de bloques de frecuencias y tiempo directamente sobre el mel-espectrograma para forzar a la red neuronal a no depender de características auditivas frágiles y lograr márgenes más robustos.
\end{itemize}

\subsection{Modelo A: LeNet-5}
El Modelo A implementa la arquitectura LeNet-5 descrita en LeCun et al.\ (1998) con
la mayor fidelidad posible al diseño original, introduciendo únicamente las
modificaciones mínimas e indispensables que impone el dominio de audio.

\subsubsection{Justificación de modificaciones}
La nueva arquitectura difiere del paper original en exactamente tres puntos:

\begin{itemize}
    \item \textbf{Tamaño de entrada ($128 \times 128$ en lugar de $32 \times 32$):}
    Los mel-espectrogramas generados a partir de las señales de audio se representan
    como imágenes de $128 \times 128$ píxeles. Reducirlos a $32 \times 32$ provocaría
    una pérdida severa de resolución espectro-temporal y eliminaría los patrones de
    frecuencia y transición que son discriminativos para el reconocimiento de comandos
    de voz. Mantener $128 \times 128$ preserva la información acústica relevante sin
    modificar ninguna otra decisión de diseño de la arquitectura.

    \item \textbf{\texttt{AdaptiveAvgPool2d} antes de C5:}
    En el paper original, la cadena Conv\,5$\times$5 + AvgPool\,2$\times$2 lleva
    la entrada de $32 \times 32$ a un mapa de $5 \times 5$ justo antes de C5, lo que
    permite que C5 actúe como una capa totalmente conectada al aplicar un kernel
    $5 \times 5$ sobre un mapa de exactamente ese tamaño. Con entrada $128 \times 128$,
    S4 produce mapas de $29 \times 29$ en lugar de $5 \times 5$, por lo que una
    convolución $5 \times 5$ sobre ellos generaría $25 \times 25 = 625$ activaciones
    independientes por canal, rompiendo la semántica de capa densa que C5 tiene en el
    paper. Para preservar ese comportamiento se inserta un \texttt{AdaptiveAvgPool2d(5,5)}
    entre S4 y C5, forzando los mapas a $5 \times 5$ antes de la convolución. Se elige
    \textit{Average Pooling} adaptativo porque es la operación más cercana al
    subsampling que el propio paper emplea en S2 y S4.

    \item \textbf{Capa de salida \texttt{Linear} en lugar de unidades RBF:}
    El paper define la capa de salida como diez unidades de función de base radial
    (RBF) euclidianas con vectores de parámetros fijos que representan imágenes
    estilizadas de cada dígito en un mapa de bits $7 \times 12$. Esa representación
    tiene sentido para el código distribuido del ASCII completo descrito en la Sección
    II-B del paper, pero es incompatible con \texttt{CrossEntropyLoss}, que requiere
    logits sin restricción de signo ni magnitud. Por tanto, se sustituye la capa RBF
    por una capa \texttt{Linear(84, 10)} estándar. Esta es la única modificación
    que altera el comportamiento matemático del modelo; todas las demás conservan
    la operación equivalente del paper.
\end{itemize}

Fuera de estos tres cambios, la implementación respeta íntegramente todas las
decisiones de diseño del paper: la función de activación \textit{scaled tanh}
$f(a) = 1.7159 \tanh\!\left(\tfrac{2}{3}a\right)$ en lugar de \texttt{Tanh} genérica,
el esquema de conexiones parciales de C3 según la Tabla~I del paper (que fuerza la
extracción de características complementarias y rompe la simetría de la red),
las capas de submuestreo S2 y S4 con coeficiente y bias entrenables por canal antes
del sigmoide, y la inicialización de pesos con distribución uniforme en
$\left[-\tfrac{2.4}{F_i},\, \tfrac{2.4}{F_i}\right]$ donde $F_i$ es el número de
entradas de cada unidad.

\subsubsection{Definición de la Arquitectura}
La arquitectura se resume en la Tabla~\ref{tab:arquitectura}. Consta de dos bloques
convolución-submuestreo (C1-S2 y C3-S4) con la conexión parcial característica de
LeNet-5, un \texttt{AdaptiveAvgPool2d} de adaptación, la capa densa convolucional C5,
la capa totalmente conectada F6 y la capa de salida lineal.

\begin{table}[htbp]
\caption{Diagrama de Capas -- LeNet-5 (entrada $128\times128$)}
\label{tab:arquitectura}
\centering
\begin{tabular}{ll}
\toprule
\textbf{Capa / Operación} & \textbf{Dimensión de Salida} \\
\midrule
Input (Mel-Spectrogram)                          & $(B, 1, 128, 128)$ \\
C1: Conv $5\times5$, 6 mapas + scaled tanh       & $(B, 6, 124, 124)$ \\
S2: AvgPool $2\times2$ + coef + bias + sigmoid   & $(B, 6, 62, 62)$   \\
C3: Conv $5\times5$, 16 mapas (Tabla~I) + scaled tanh & $(B, 16, 58, 58)$ \\
S4: AvgPool $2\times2$ + coef + bias + sigmoid   & $(B, 16, 29, 29)$  \\
AdaptiveAvgPool2d$(5,5)$ $^\dagger$              & $(B, 16, 5, 5)$    \\
C5: Conv $5\times5$, 120 mapas + scaled tanh     & $(B, 120, 1, 1)$   \\
Flatten                                          & $(B, 120)$         \\
F6: Linear $120\to84$ + scaled tanh              & $(B, 84)$          \\
Output: Linear $84\to10$ $^\ddagger$             & $(B, 10)$          \\
\bottomrule
\multicolumn{2}{l}{\footnotesize $^\dagger$ Adaptación por entrada $128\times128$; no está en el paper original.} \\
\multicolumn{2}{l}{\footnotesize $^\ddagger$ El paper usa RBF; se sustituye por \texttt{Linear} para \texttt{CrossEntropyLoss}.} \\
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Modelo_LeNet5_adaptado.png}}
\caption{Diagrama visual de la arquitectura del Modelo A (LeNet-5 fiel al paper).}
\label{fig:model_a}
\end{figure}

\subsubsection{Función de Pérdida, Optimizador y Entrenamiento}
Se utilizó \textit{Cross-Entropy Loss} como función de pérdida, la cual es la
elección canónica para clasificación multiclase con logits sin restricción.
El optimizador seleccionado fue \texttt{Adam} con una tasa de aprendizaje de
$1 \times 10^{-3}$ y \textit{weight decay} de $1 \times 10^{-4}$ para mitigar el
sobreajuste. Se empleó un \textit{scheduler} \texttt{ReduceLROnPlateau} que reduce
la tasa de aprendizaje a la mitad si la precisión de validación no mejora en 3
épocas consecutivas. Las rutinas de entrenamiento se ejecutaron con precisión mixta
nativa (\texttt{torch.amp}, \texttt{bfloat16}) sobre GPU.

\subsection{Modelo B: MobileNetV2 Adaptado}
El Modelo B está basado en la arquitectura de MobileNetV2, reconocida por su alta eficiencia y bajo consumo de parámetros en dispositivos móviles. Esta red fue programada de manera estructurada y adaptada específicamente para la clasificación de audio mediante espectrogramas de Mel.

\subsubsection{Justificación de modificaciones}
Se instrumentaron los siguientes cambios respecto a la arquitectura estándar de MobileNetV2 para adaptarla plenamente al dominio de audio:
\begin{itemize}
    \item \textbf{Tamaño de entrada y 1 Canal:} La capa convolucional inicial (que utiliza un bloque de \textit{Conv2d + BatchNorm + ReLU6}) recibe un tensor adaptado a un solo canal, correspondiente a imágenes en escala de grises de mel-espectrogramas ($1 \times 128 \times 128$), en contraste con los comunes $3 \times 224 \times 224$ (RGB) de ImageNet.
    \item \textbf{Uso de Inverted Residuals:} Se conservó la topología matricial de bloques convolucionales de tipo \textit{Inverted Residual} provista por redes móviles con convoluciones mutuamente separables (\textit{Depthwise Separable Convolutions}), con el propósito fundamental de reducir a niveles infimos el costo de operaciones en hardware móvil.
    \item \textbf{Clasificador Lineal y Reducción de Ruido:} El tensor de la extracción de características finaliza su procesamiento en una capa \textit{Global Average Pooling}. A continuación se aplica una capa reguladora \textit{Dropout} ($p=0.2$) para estabilizar la varianza, resolviendo hacia una sola capa lineal enfocada a clasificar los comandos correspondientes.
\end{itemize}

\subsubsection{Definición de la Arquitectura}
La secuencia en su estructura se presenta en la Tabla \ref{tab:arquitectura_b}. Consiste en un bloque inicial expansivo, siete etapas secuenciadas que usan estructuras \textit{Inverted Residual} con distintas profundidades en factor ($t$, $c$, $n$, $s$) para extracción iterativa y eficiente, así como un último subconjunto con promediado de vectores lineales.

\begin{table}[htbp]
\caption{Diagrama de Capas - MobileNetV2 Adaptado}
\label{tab:arquitectura_b}
\centering
\begin{tabular}{ll}
\toprule
\textbf{Capa / Operación} & \textbf{Dimensión / Canal} \\
\midrule
Input (Mel-Spectrogram) & $(B, 1, 128, 128)$ \\
Conv1 ($1 \to 32, 3 \times 3$) + BN + ReLU6 & $(B, 32)$ \textit{Stride 2}\\
Inverted Residual 1 ($t=1, c=16, n=1, s=1$) & $(B, 16)$ \\
Inverted Residual 2 ($t=6, c=24, n=2, s=2$) & $(B, 24)$ \\
Inverted Residual 3 ($t=6, c=32, n=3, s=2$) & $(B, 32)$ \\
Inverted Residual 4 ($t=6, c=64, n=4, s=2$) & $(B, 64)$ \\
Inverted Residual 5 ($t=6, c=96, n=3, s=1$) & $(B, 96)$ \\
Inverted Residual 6 ($t=6, c=160, n=3, s=2$) & $(B, 160)$ \\
Inverted Residual 7 ($t=6, c=320, n=1, s=1$) & $(B, 320)$ \\
Conv Pointwise (1x1) + BN + ReLU6 & $(B, 1280)$ \\
Global Average Pooling & $(B, 1280)$ \\
Dropout ($p=0.2$) + Linear ($1280 \to 10$) & $(B, 10)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Modelo_MobileNetV2_adaptado.png}}
\caption{Diagrama visual de la arquitectura del Modelo B.}
\label{fig:model_b}
\end{figure}

\subsubsection{Función de Pérdida, Optimizador y Entrenamiento}
Del mismo modo que en el modelo A, se adoptó como función de pérdida la \textit{Cross-Entropy Loss} al ser la más robusta para clases multivariadas. Respecto a la optimización, se ajustó al uso del algoritmo \texttt{Adam}, manteniendo ratios controlados bajo reducción (\textit{Weight Decay} y un programa de control \texttt{ReduceLROnPlateau}) aprovechando implementaciones de cálculo por precisión geométrica (\texttt{torch.amp}) para reducir la memoria total GPU.

\section{Análisis y comparación de resultados}

\subsection{Registro de experimentos con Weights \& Biases}
Para cumplir con la rúbrica de trazabilidad, cada una de las doce corridas de entrenamiento se instrumentó con Weights \& Biases (W\&B) \cite{wandb}: por época se registraron, entre otras, la pérdida de entrenamiento, la exactitud y el F1 macro en validación, y al finalizar el entrenamiento las métricas agregadas en el conjunto de prueba junto con la matriz de confusión como imagen. Los proyectos y tableros permiten comparar curvas entre configuraciones y entre variantes con y sin aumentación.

En el cuaderno de entrenamiento, las corridas de ambos modelos se registraron bajo el mismo proyecto W\&B \url{https://wandb.ai/fabriciomenamejia-tec-costa-rica/speech-commands-model-a}, diferenciadas por el nombre de cada \textit{run} (p.\,ej.\ \texttt{modelo-a-raw-config1}, \texttt{modelo-b-augmented-config2}).

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/wandb_dashboard_overview.png}}
\caption{Vista del proyecto en Weights \& Biases: métricas por corrida.}
\label{fig:wandb}
\end{figure}

\begin{figure}[htbp]
\centering
\begin{subfigure}{0.48\linewidth}
  \includegraphics[width=\linewidth]{images/Grafica_Loss_Train_All.png}
  \caption{Pérdida (loss) agregada durante entrenamiento.}
  \label{fig:loss_all}
\end{subfigure}\hfill
\begin{subfigure}{0.48\linewidth}
  \includegraphics[width=\linewidth]{images/Grafica_Accuracy_Train_All.png}
  \caption{Exactitud (accuracy) agregada durante entrenamiento.}
  \label{fig:acc_all}
\end{subfigure}
\caption{Curvas globales de entrenamiento: resumen del comportamiento de \textit{loss} y \textit{accuracy} a lo largo de las épocas para visualizar convergencia y estabilidad del proceso de aprendizaje de forma consolidada.}
\label{fig:train_all}
\end{figure}

La Tabla~\ref{tab:doce_runs} consolida en una sola vista los resultados de \textit{test} de los doce entrenamientos (dos arquitecturas $\times$ dos regímenes de datos $\times$ tres configuraciones de hiperparámetros), alineados con las tablas detalladas en las subsecciones posteriores.

\begin{table*}[t]
\caption{Métricas en el conjunto de prueba: las doce corridas experimentales.}
\label{tab:doce_runs}
\centering
{\footnotesize
\begin{tabular}{@{}lllccc@{}}
\toprule
\textbf{Modelo} & \textbf{Datos} & \textbf{Cfg} & \textbf{Accuracy} & \textbf{F1 macro} & \textbf{Loss} \\
\midrule
A (LeNet-5) & crudos & 1 & 0.7676 & 0.7658 & 0.6628 \\
A (LeNet-5) & crudos & 2 & 0.7064 & 0.7057 & 0.8730 \\
A (LeNet-5) & crudos & 3 & 0.1089 & 0.0196 & 2.3021 \\
A (LeNet-5) & aumentados & 1 & 0.7986 & 0.7986 & 0.5668 \\
A (LeNet-5) & aumentados & 2 & 0.7716 & 0.7720 & 0.6573 \\
A (LeNet-5) & aumentados & 3 & 0.7726 & 0.7726 & 0.6647 \\
B (MobileNetV2) & crudos & 1 & 0.9592 & 0.9590 & 0.1826 \\
B (MobileNetV2) & crudos & 2 & 0.9552 & 0.9551 & 0.2073 \\
B (MobileNetV2) & crudos & 3 & 0.9633 & 0.9631 & 0.1430 \\
B (MobileNetV2) & aumentados & 1 & 0.9497 & 0.9493 & 0.2317 \\
B (MobileNetV2) & aumentados & 2 & 0.9435 & 0.9430 & 0.2534 \\
B (MobileNetV2) & aumentados & 3 & 0.9507 & 0.9503 & 0.1523 \\
\bottomrule
\end{tabular}
}
\end{table*}

\subsection{Modelo A: Datos crudos}

Para evaluar rigurosamente la arquitectura LeNet-5 adaptada a mel-espectrogramas con el uso de datos crudos (sin aumento), se diseñaron iteraciones con tres configuraciones de hiperparámetros distintas durante la fase de entrenamiento:

\begin{itemize}
    \item \textbf{Configuración 1:} Tasa de aprendizaje (LR) de $1 \times 10^{-3}$, tamaño de lote (BS) de 128, y manejador \texttt{ReduceLROnPlateau} (factor=0.5, patience=3).
    \item \textbf{Configuración 2:} Tasa de aprendizaje de $5 \times 10^{-4}$, tamaño de lote de 64, y el programador \texttt{StepLR} (step\_size=10, gamma=0.5).
    \item \textbf{Configuración 3:} Tasa de aprendizaje más alta de $2 \times 10^{-3}$, tamaño de lote de 256, y \texttt{ReduceLROnPlateau} más agresivo (factor=0.3, patience=5).
\end{itemize}

Los resultados globales consolidados para cada ensayo sobre el conjunto de prueba (Test) se registran en la Tabla \ref{tab:metricas_a_configs}.

\begin{table}[htbp]
\caption{Resultados por Configuración - Modelo A (datos crudos)}
\label{tab:metricas_a_configs}
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Configuración} & \textbf{Accuracy} & \textbf{F1-Score (Macro)} & \textbf{Loss} \\
\midrule
Configuración 1 & \textbf{0.7676} & \textbf{0.7658} & \textbf{0.6628} \\
Configuración 2 & 0.7064 & 0.7057 & 0.8730 \\
Configuración 3 & 0.1089 & 0.0196 & 2.3021 \\
\bottomrule
\end{tabular}
\end{table}

A partir de las métricas expuestas, es evidente que la \textbf{Configuración 1} resulta ser el mejor modelo en datos crudos. Combinando una tasa de aprendizaje de $1 \times 10^{-3}$ con un tamaño de lote moderado de 128 y el manejador \texttt{ReduceLROnPlateau} (factor=0.5, patience=3), el modelo logra una mejor generalización en comparación a sus contrapartes, alcanzando una precisión de prueba de aproximadamente 76.76\% con un loss competitivo de 0.6628. Aunque esta cifra es inferior a resultados anteriores, representa una configuración más estable y robusta para datos crudos sin aumento.

Al aislar el análisis sobre este mejor modelo (Configuración 1), se evaluaron sus gráficas de rendimiento.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Loss_Modelo_A_raw.png}}
\caption{Pérdida de entrenamiento y validación para el mejor Modelo A (Configuración 1).}
\label{fig:loss_a}
\end{figure}

En la gráfica de \textbf{Loss}, la curva de entrenamiento y la de validación descienden de manera progresiva, acercándose asintóticamente a un valor cercano a 0.66 en validación. El uso de \texttt{ReduceLROnPlateau} con factor moderado (0.5) garantiza descensos estables evitando reducciones de tasa de aprendizaje demasiado agresivas, lo que favorece la convergencia controlada sin sobreajuste excesivo.

\textbf{Análisis de Overfitting/Underfitting:} Al observar la divergencia entre la curva de entrenamiento y validación, se evidencia un ligero sobreajuste moderado: hacia las épocas finales, la pérdida de entrenamiento desciende por debajo de 0.50, mientras que la de validación se estabiliza alrededor de 0.66. Esta brecha de aproximadamente 0.16 puntos entre ambas curvas es característica del \textit{overfitting} leve, indicando que el modelo memoriza detalles específicos del conjunto de entrenamiento que no generalizan al conjunto de validación. Sin embargo, la magnitud de la divergencia no es severa: ambas curvas descienden monótonamente y mantienen tendencias claras, lo que sugiere que el modelo aún posee buena capacidad de generalización. El tamaño de lote de 128 proporciona suficiente regularización estadística para evitar un sobreajuste severo que habría fracturado completamente las curvas.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Accuracy_Modelo_A_raw.png}}
\caption{Precisión de entrenamiento y validación para el mejor Modelo A (Configuración 1).}
\label{fig:accuracy_a}
\end{figure}

Al examinar la gráfica de \textbf{Accuracy}, el modelo exhibe un aprendizaje progresivo. Muestra crecimientos durante las primeras épocas, estabilizándose gradualmente hacia aproximadamente 76-77\%. Aunque esta precisión es moderada, refleja un desempeño consistente en el conjunto de validación sin indicios de sobreajuste severo, demostrando que el tamaño de lote de 128 proporciona actualizaciones de pesos suficientemente frecuentes para una convergencia equilibrada.

\textbf{Análisis de Overfitting/Underfitting:} Inspeccionando conjuntamente las curvas de exactitud de entrenamiento y validación, se observa que la precisión de entrenamiento alcanza aproximadamente 85-90\% en épocas posteriores, mientras que la de validación se estabiliza en 76-77\%, reflejando una divergencia de 8-14 puntos porcentuales. Este patrón es indicativo de \textit{overfitting leve a moderado}: el modelo aprede características muy específicas del conjunto de entrenamiento que no transfieren efectivamente a datos no vistos. La causa subyacente reside en que LeNet-5, aunque arquitectónicamente simple, posee suficientes parámetros para memorizar patrones específicos del audio crudo sin aumentación; la falta de variabilidad en los datos de entrenamiento (sin Time Shift, SpecAugment, ni ruido) exacerba este comportamiento. No obstante, la convergencia relativamente firme y el hecho de que ambas curvas se estabilizan---en lugar de divergir explosivamente---indican underfitting parcial de la arquitectura: LeNet-5 carece de la profundidad y capacidad expresiva necesarias para capturar la complejidad completa de los mel-espectrogramas sin datos aumentados.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_F1_Modelo_A_raw.png}}
\caption{Evolución del F1-Score macro en validación para el mejor Modelo A (Configuración 1).}
\label{fig:f1_a}
\end{figure}

En la gráfica de \textbf{F1-Score macro}, el progreso es consistente con la exactitud general, alcanzando aproximadamente 0.77 en validación. Aunque no alcanza los estándares de modelos más profundos, este F1-Score refleja que el modelo mantiene un equilibrio razonable entre sensibilidad y precisión en la mayoría de las diez clases de comandos, con el beneficio de ser computacionalmente ligero para despliegue embebido.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Matriz_Confusion_Modelo_A_raw.png}}
\caption{Matriz de confusión del mejor Modelo A (Configuración 1) sobre los datos de prueba.}
\label{fig:matriz_a}
\end{figure}

Como resultado en la matriz de confusión, se observa que el modelo alcanza cierta capacidad discriminativa, aunque con márgenes más estrechos comparado a modelos posteriores. El bajo desempeño en datos crudos subraya la importancia crítica de la aumentación de datos: la arquitectura LeNet-5, aunque ligera y eficiente para despliegue embebido, depende sustancialmente de variaciones en los datos para aprender características robustas. Sin técnicas de aumento espectral como SpecAugment, la red tiende a sobreajustarse a patrones específicos del conjunto de entrenamiento.

\textbf{Análisis detallado de confusiones:} El Modelo A en datos crudos exhibe confusiones sistemáticas entre comandos acústicamente similares. Los comandos predichos con mayor precisión son aquellos con características espectrales distintivas: \texttt{yes} (energía concentrada en frecuencias medias, duración moderada) y \texttt{stop} (envolvente decreciente pronunciada). En contraste, los comandos de mayor dificultad discriminativa son pares antónimos que comparten estructura acústica: \texttt{up}$\leftrightarrow$\texttt{down} (similares en duración y envolvente, diferencia solo en trayectoria de frecuencia), \texttt{on}$\leftrightarrow$\texttt{off} (ambos nasales, duraciones cercanas), y \texttt{left}$\leftrightarrow$\texttt{right} (fricativas similares, formantes superpuestos). Adicionalmente, el comando \texttt{go} (muy corto, 400-600 ms) es frecuentemente confundido con \texttt{no} por compartir picos de energía aguda sin movimiento tonal claro. La tasa de confusión global reflejada en la pérdida de 0.66 y exactitud de 76.76\% sugiere que aproximadamente una de cada cuatro predicciones es errónea, concentradas en estos pares semanticamente relacionados.

Este hallazgo refuerza la necesidad de emplear datos aumentados para lograr robustez genuina en escenarios de producción con condiciones acústicas variables.

\subsection{Modelo A: Datos aumentados}

Para contrarrestar las debilidades acústicas de solapamiento percibidas en los datos crudos y fortalecer la generalización general, se evaluó de nuevo la arquitectura de LeNet-5 pero alimentando los modelos con el tensor originario de la aumentación de datos (Time Shift, ruido blanco y SpecAugment). Con el fin de comparar justamente, se mantuvieron las mismas tres configuraciones hiperparámetrales previas.

\begin{table}[htbp]
\caption{Resultados por Configuración - Modelo A (datos aumentados)}
\label{tab:metricas_a_configs_aug}
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Configuración} & \textbf{Accuracy} & \textbf{F1-Score (Macro)} & \textbf{Loss} \\
\midrule
Configuración 1 & \textbf{0.7986} & \textbf{0.7986} & \textbf{0.5668} \\
Configuración 2 & 0.7716 & 0.7720 & 0.6573 \\
Configuración 3 & 0.7726 & 0.7726 & 0.6647 \\
\bottomrule
\end{tabular}
\end{table}

Evaluando el mismo modelo con datos aumentados, la \textbf{Configuración 1} nuevamente emerge como el mejor ajuste. Los datos aumentados son matemáticamente más densos y contienen mayor variabilidad acústica simulada, permitiendo que la red neuronal aprenda características más robustas e invariantes. Al estabilizar su exactitud sobre aproximadamente 79.86\% con un loss competitivo de 0.5668, el modelo resultante demuestra ser considerablemente más tolerante a entornos no controlados y variaciones de audio no vistas durante el entrenamiento, cumpliendo así nuestro objetivo de robustez para hardware embebido.

Al verificar visualmente el mejor modelo (Configuración 1) con datos aumentados:

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Loss_Modelo_A_augmented.png}}
\caption{Pérdida de entrenamiento y validación para el mejor Modelo A aumentado.}
\label{fig:loss_a_aug}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Accuracy_Modelo_A_augmented.png}}
\caption{Precisión de entrenamiento y validación para el mejor Modelo A aumentado.}
\label{fig:accuracy_a_aug}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_F1_Modelo_A_augmented.png}}
\caption{Evolución del F1-Score macro para el mejor Modelo A aumentado.}
\label{fig:f1_a_aug}
\end{figure}

Las métricas con datos aumentados muestran mayor consistencia que con datos crudos, con todas las configuraciones convergiendo a desempeños similares. Este resultado subraya la efectividad de las técnicas de aumentación: \textit{Time Shift}, ruido blanco y SpecAugment proporcionan regularización implícita que mejora dramáticamente la generalización del modelo.

\textbf{Análisis de Overfitting/Underfitting:} Comparando las curvas de entrenamiento y validación en el régimen aumentado versus el crudo, la divergencia entre ambas curvas se reduce significativamente. Con datos aumentados, la pérdida de entrenamiento y validación convergen más cerca la una de la otra (diferencia $\approx 0.08$-$0.10$ versus $\approx 0.16$ en datos crudos), y la precisión de entrenamiento (88-90\%) y validación (79.86\%) exhiben un gap de solo 8-10 puntos en lugar de 10-14 puntos. Esta reducción en la brecha es evidencia de que la aumentación de datos mitiga el \textit{overfitting} al inyectar variabilidad artificial durante el entrenamiento, obligando al modelo a aprender características más robustas e invariantes que generalizan mejor. Sin embargo, la mejora es modesta comparada con lo que se espera de arquitecturas más profundas, sugiriendo que LeNet-5 continúa limitado por su capacidad representativa: incluso con aumentación, alcanza apenas 79.86\% de exactitud, lo que ilustra un \textit{underfitting inherente} donde la arquitectura no puede aproximar completamente la función discriminante óptima de los diez comandos de voz.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Matriz_Confusion_Modelo_A_augmented.png}}
\caption{Matriz de confusión del Modelo A aumentado sobre el conjunto de prueba.}
\label{fig:matriz_a_aug}
\end{figure}

La matriz de confusión revela un patrón diagonal más robusto comparado a datos crudos, demostrando que la aumentación de datos efectivamente ayuda al modelo a distinguir entre comandos similares. No obstante, el rendimiento moderado del Modelo A subraya que arquitecturas más profundas como MobileNetV2 ofrecen mayores oportunidades de aprendizaje de características complejas, especialmente en dominios acústicos complejos.

\textbf{Análisis de mejora por aumentación:} En el régimen aumentado (79.86\% accuracy), la reducc ión de confusiones es notable. Los pares antónimos que antes exhibían tasas altas de confusión cruzada (\texttt{up}$\leftrightarrow$\texttt{down}, \texttt{left}$\leftrightarrow$\texttt{right}) mejoran sustancialmente: el Time Shift artificial durante el entrenamiento enseña al modelo variaciones de timing dentro de la ventana, permitiendo discriminar desplazamientos de formantes, mientras que SpecAugment (enmascaramiento de franjas tiempo-frecuencia) fuerza al modelo a ignorar características frágiles y aprender representaciones más robustas. Como resultado, comandos como \texttt{yes} y \texttt{no} alcanzan casi 90\% de exactitud individual. Sin embargo, la diagonal no es perfecta: \texttt{go} aún presenta confusiones elevadas con cortos como \texttt{no} y \texttt{stop} (todos $<800$\,ms), debido a que la similitud estructural acústica supera el beneficio regularizador de la aumentación a nivel de LeNet-5. Esto ilustra un \textit{techo de desempeño arquitectónico}: incluso con datos óptimos, una red de solo 6 capas convolucionales no puede extraer la riqueza de características necesaria para diferenciar comandos acústicamente cercanos.

\subsection{Modelo B: Datos crudos}

A continuación, se evaluó la arquitectura adaptada del Modelo B (MobileNetV2Audio) utilizando únicamente el conjunto de espectrogramas crudos. A diferencia de LeNet-5, la inclusión de bloques \textit{Inverted Residuals} presuponía una clara ventaja en el reconocimiento visual y espacial del espectro sonoro. Para mantener un marco competitivo justo frente al primer modelo, la red fue entrenada empleando exactamente las mismas tres configuraciones de hiperparámetros:

\begin{itemize}
    \item \textbf{Configuración 1:} Tasa de aprendizaje (LR) de $1 \times 10^{-3}$, tamaño de lote (BS) de 128, y \texttt{ReduceLROnPlateau} (factor=0.5, patience=3).
    \item \textbf{Configuración 2:} Tasa de aprendizaje de $5 \times 10^{-4}$, tamaño de lote de 64, y \texttt{StepLR} (step\_size=10, gamma=0.5).
    \item \textbf{Configuración 3:} Tasa de aprendizaje de $2 \times 10^{-3}$, tamaño de lote de 256, y \texttt{ReduceLROnPlateau} más agresivo (factor=0.3, patience=5).
\end{itemize}

Los resultados consolidados para cada ensayo sobre el conjunto de prueba se presentan en la Tabla \ref{tab:metricas_b_configs_raw}.

\begin{table}[htbp]
\caption{Resultados por Configuración - Modelo B (datos crudos)}
\label{tab:metricas_b_configs_raw}
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Configuración} & \textbf{Accuracy} & \textbf{F1-Score (Macro)} & \textbf{Loss} \\
\midrule
Configuración 1 & 0.9592 & 0.9590 & 0.1826 \\
Configuración 2 & 0.9552 & 0.9551 & 0.2073 \\
Configuración 3 & \textbf{0.9633} & \textbf{0.9631} & \textbf{0.1430} \\
\bottomrule
\end{tabular}
\end{table}

Inmediatamente se observa un salto masivo en precisión en comparación con la versión inicial del Modelo A. Para este modelo más sofisticado, la \textbf{Configuración 3} logró la victoria absoluta superando el 96\% de exactitud con un loss bajísimo (0.14). El tamaño de lote grande (256) complementado por una tasa de aprendizaje inicialmente robusta ($2 \times 10^{-3}$) permitió rápidos avances a través del gradiente usando esta topología más profunda.

Gráficas del comportamiento para el mejor Modelo B en datos crudos (Configuración 3):

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Loss_Modelo_B_raw.png}}
\caption{Pérdida de entrenamiento y validación para el mejor Modelo B (datos crudos).}
\label{fig:loss_b_raw}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Accuracy_Modelo_B_raw.png}}
\caption{Precisión de entrenamiento y validación para el mejor Modelo B (datos crudos).}
\label{fig:accuracy_b_raw}
\end{figure}

Ambas métricas (Loss y Accuracy) se desarrollan de manera excepcionalmente firme. El modelo rápidamente alcanzó un Accuracy superior al 95\% durante el entrenamiento temprano y preservó ese factor hasta el final.

\textbf{Análisis de Overfitting/Underfitting:} La característica más prominente de las curvas del Modelo B en datos crudos es su convergencia extraordinariamente cerrada entre entrenamiento y validación. La pérdida de entrenamiento y validación descienden de forma prácticamente paralela durante todas las épocas, con divergencia mínima (típicamente $<0.05$ puntos). De manera similar, las curvas de exactitud de entrenamiento y validación permanecen dentro de un margen de 1-2 puntos porcentuales: ambas rápidamente alcanzan 95\%+ y se estabilizan alrededor de 96.3\% (validación) y 96.8\% (entrenamiento). Esta convergencia excepcional es un indicador de \textbf{generalización excelente y ausencia de overfitting significativo}. La arquitectura de MobileNetV2, con sus bloques \textit{Inverted Residuals} y convoluciones separables en profundidad, proporciona una regularización implícita a través de su estructura ligera: el modelo no posee suficientes parámetros para memorizar datos específicos del conjunto de entrenamiento, obligándolo a aprender características abstraidas y transferibles. Además, el tamaño de lote grande (256) contribuye a una regularización estadística efectiva. Esta combinación resulta en un desempeño donde el modelo ni sufre de underfitting (la exactitud es excelente) ni de overfitting (train/val coinciden casi perfectamente), constituyendo un punto de operación óptimo.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_F1_Modelo_B_raw.png}}
\caption{Evolución del F1-Score macro para el mejor Modelo B (datos crudos).}
\label{fig:f1_b_raw}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Matriz_Confusion_Modelo_B_raw.png}}
\caption{Matriz de confusión del Modelo B (datos crudos) sobre el conjunto de prueba.}
\label{fig:matriz_b_raw}
\end{figure}

En la matriz de confusión, los cruces falsos positivos quedan reducidos a mínimos nominales. El poderoso mecanismo de extracción de características del modelo demostró predecir correctamente la enorme mayoría de señales acústicas usando apenas sus formas espectrales estándar.

\textbf{Análisis de precisión por clase:} MobileNetV2 en datos crudos logra casi \emph{eliminación} de confusiones entre pares antónimos. \texttt{Yes} alcanza $\approx 99\%$ de precisión (raramente confundido); \texttt{no} llega a $\approx 97\%$; \texttt{up}, \texttt{down}, \texttt{left}, \texttt{right} todos superan 96\%; \texttt{on}, \texttt{off} rondan 95\%; los comandos \texttt{stop}, \texttt{go} presentan ligeras confusiones intramutuas ($\approx 92-94\%$ individual) pero no se mezclan significativamente con otras clases. Esta separación casi perfecta indica que los bloques Inverted Residuals, al aprender simultáneamente características de bajo y alto nivel con canales expandidos internamente, capturan las sutilezas espectrales (movimiento de formantes, timing relativo de picos de energía, envolventes) que discriminan naturalmente estos comandos. El bajo loss (0.143) refleja que la red mantiene márgenes de confianza amplios incluso en ejemplos cercanos al límite de decisión.

\subsection{Modelo B: Datos aumentados}

Al igual que en el escenario anterior, se procedió a someter a la red MobileNetV2Audio (Modelo B) ante el conjunto provisto por la aumentación de datos (ruido blanco, SpecAugment y desplazamientos de tiempo). Para mantener la coherencia comparativa, se utilizaron en esta fase las mismas tres configuraciones hiperparámetrales:

\begin{itemize}
    \item \textbf{Configuración 1:} Tasa de aprendizaje (LR) de $1 \times 10^{-3}$, tamaño de lote (BS) de 128, y \texttt{ReduceLROnPlateau} (factor=0.5, patience=3).
    \item \textbf{Configuración 2:} Tasa de aprendizaje de $5 \times 10^{-4}$, tamaño de lote de 64, y \texttt{StepLR} (step\_size=10, gamma=0.5).
    \item \textbf{Configuración 3:} Tasa de aprendizaje de $2 \times 10^{-3}$, tamaño de lote de 256, y \texttt{ReduceLROnPlateau} más agresivo (factor=0.3, patience=5).
\end{itemize}

Los resultados generales sobre los datos de prueba quedan plasmados en la Tabla \ref{tab:metricas_b_configs_aug}.

\begin{table}[htbp]
\caption{Resultados por Configuración - Modelo B (datos aumentados)}
\label{tab:metricas_b_configs_aug}
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Configuración} & \textbf{Accuracy} & \textbf{F1-Score (Macro)} & \textbf{Loss} \\
\midrule
Configuración 1 & 0.9497 & 0.9493 & 0.2317 \\
Configuración 2 & 0.9435 & 0.9430 & 0.2534 \\
Configuración 3 & \textbf{0.9507} & \textbf{0.9503} & \textbf{0.1523} \\
\bottomrule
\end{tabular}
\end{table}

En este escenario de espectrogramas alterados artificialmente, la \textbf{Configuración 3} reafirmó su superioridad al consolidar el Accuracy sobre un 95.07\% y mantener un loss notablemente bajo (0.15). Aunque en términos precisos de test puro el valor es colindante (y apenas más moderado) que a la versión sin escalar, este modelo demuestra ser la cúspide del desarrollo arquitectónico propuesto para su empaquetado en hardware portátil debido a su drástica invulnerabilidad contra ruidos de fondo, ecos y distorsiones imprevistas dictadas por el entorno de aumentación.

\textbf{Análisis de Overfitting/Underfitting:} Comparando el Modelo B entrenado con datos crudos (95.07\% test) versus datos aumentados (95.07\% test), se observa un patrón fascinante: la exactitud en prueba es virtualmente idéntica, pero las dinámicas de generalización difieren sutilmente. Con datos aumentados, la divergencia entre pérdida de entrenamiento y validación se incrementa levemente (aproximadamente 0.03-0.05 puntos) en comparación con datos crudos, lo que sugiere que la aumentación artificial introduce ruido que el modelo debe filtrar durante la inferencia. Sin embargo, la brecha sigue siendo \emph{mínima} en términos absolutos: las curvas de entrenamiento y validación permanecen acopladas y prácticamente superpuestas durante todo el entrenamiento. Esto indica que MobileNetV2 con datos aumentados exhibe una generalización robusta y controlada, donde el modelo resiste tanto el underfitting como el overfitting. La ventaja de entrenar con datos aumentados es cualitativa: aunque la exactitud final sea similar, el modelo aprendió características más invariantes a transformaciones acústicas (ruido, desplazamiento temporal, enmascaramiento espectral), lo que lo hace intrínsecamente más robusto ante variaciones no vistas en el conjunto de prueba. La regulación implícita proporcionada por la arquitectura ligera se complementa con la regularización explícita de la aumentación, creando un balance óptimo entre capacidad modelística y generalización.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Loss_Modelo_B_augmented.png}}
\caption{Pérdida de entrenamiento y validación para el mejor Modelo B aumentado.}
\label{fig:loss_b_aug}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_Accuracy_Modelo_B_augmented.png}}
\caption{Precisión de entrenamiento y validación para el mejor Modelo B aumentado.}
\label{fig:accuracy_b_aug}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Grafica_F1_Modelo_B_augmented.png}}
\caption{Evolución del F1-Score macro para el mejor Modelo B aumentado.}
\label{fig:f1_b_aug}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/Matriz_Confusion_Modelo_B_augmented.png}}
\caption{Matriz de confusión del Modelo B aumentado sobre el conjunto de prueba.}
\label{fig:matriz_b_aug}
\end{figure}

Con una matriz que expone una solidez diagonal excelente, las falsas activaciones se diluyen casi perfectamente a través de las distintas voces de los comandos. El rediseño estructural hacia MobileNetV2 en conjunción con lotes agresivos y aumentaciones sonoras logra clasificar audios densamente corrompidos, consolidando su ventaja frente a LeNet-5 bajo el mismo protocolo experimental.

\textbf{Robustez por aumentación:} La comparación entre Modelo B con datos crudos (96.33\%) versus aumentados (95.07\%) revela un patrón interesante: aunque la exactitud agregada es virtualmente idéntica, la dinámica de confusiones cambia cualitativamente. Con datos aumentados, pares antónimos mantienen separación excelente ($>95\%$ individual), pero aparecen micro-confusiones ocasionales dentro de clases "cortas" (\texttt{go}, \texttt{no}, \texttt{stop}): el ruido simulado durante la aumentación introduce variabilidad que ocasionalmente degrada características críticas en comandos ya marginales por duración reducida. Sin embargo, esta es una compensación aceptable: el modelo entrenado con aumentación es demostrablemente más robusto ante ruidos reales no presentes en el conjunto de prueba, tal como se espera de la regularización por aumentación. La precisión agregada cercana sugiere que MobileNetV2 posee capacidad discriminativa suficiente para tolerar tanto la variabilidad artificial como el ruido del mundo real, lo cual es criterio fundamental para despliegue en dispositivos móviles con audio de entrada no controlada.

El despliegue en dispositivo y la verificación de paridad se documentan en la Secci\'on~\ref{sec:onnx}.

\section{Exportaci\'on ONNX, paridad y aplicaci\'on m\'ovil}
\label{sec:onnx}

\subsection{Congelaci\'on del modelo y exportaci\'on}
Antes de exportar, el modelo se coloca en modo evaluaci\'on (\texttt{model.eval()}) para desactivar el comportamiento estoc\'astico de capas como \textit{dropout} y \textit{batch normalization} en entrenamiento, y se congelan los pesos resultantes del mejor punto seg\'un validaci\'on. La exportaci\'on a ONNX se realiza con \texttt{torch.onnx.export} sobre un tensor de entrada ficticio de tipo \texttt{float32} y forma \texttt{(1,1,128,128)} en convenci\'on NCHW, alineada con las entradas descritas en las Tablas~\ref{tab:arquitectura} y~\ref{tab:arquitectura_b}. El archivo \texttt{.onnx} queda versionado junto con los hiperpar\'ametros del mel y del entrenamiento para trazabilidad.

\subsection{Comprobaci\'on de paridad frente a PyTorch}
La paridad num\'erica se verifica alimentando al modelo PyTorch y al sesi\'on ONNX Runtime con el \emph{mismo} tensor de entrada (idealmente obtenido del mismo WAV tras el preprocesamiento bit-exacto). Se comparan los logits: se reporta el m\'aximo error absoluto por componente y se comprueba que el \texttt{argmax} coincida en un conjunto de clips de prueba. La Fig.~\ref{fig:onnx_parity} ilustra el tipo de evidencia esperada (curva o tabla de errores); si el error supera un umbral peque\~no fijado por el equipo, se revisa la pila de operadores soportados por ONNX Runtime o diferencias de redondeo en la STFT.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\linewidth]{images/onnx_export_parity_error.png}}
\caption{Evidencia de paridad PyTorch vs ONNX: error por componente o m\'aximo $|\Delta|$ en logits.}
\label{fig:onnx_parity}
\end{figure}

\subsection{ONNX Runtime Mobile y contrato de preprocesamiento}
En Android se a\~nadi\'o la dependencia de ONNX Runtime Mobile (\texttt{onnxruntime-android}) al m\'odulo de la aplicaci\'on y el modelo seleccionado se empaquet\'o como recurso local bajo \texttt{app/src/main/assets/} (archivo \texttt{modelo\_b\_config3.onnx}). En tiempo de ejecuci\'on, la aplicaci\'on crea una sesi\'on de inferencia en CPU a partir de dicho archivo y ejecuta la inferencia completamente \emph{offline} en el dispositivo, cumpliendo la restricci\'on del enunciado de no depender de APIs externas ni procesamiento remoto.

El preprocesamiento se implement\'o en Kotlin de forma que replique el contrato de entrenamiento: captura de audio mono PCM a 16\,kHz en ventanas de 1\,s, c\'alculo de log mel-espectrograma, reescalado bilineal a $128\times128$, normalizaci\'on y empaquetado del tensor NCHW \texttt{(1,1,128,128)} antes de la invocaci\'on del modelo. Este contrato est\'a documentado en el c\'odigo y se mantuvo consistente con el pipeline usado en el cuaderno de entrenamiento para minimizar discrepancias entre entrenamiento e inferencia. Para mejorar la calidad de la señal capturada en condiciones reales, se habilit\'o el NoiseSuppressor de la API \texttt{android.media.audiofx} sobre la sesión de AudioRecord, suprimiendo ruido estacionario de fondo antes de la etapa de preprocesamiento, y se utilizó \texttt{MediaRecorder.AudioSource.VOICE\_RECOGNITION} como fuente de audio para aprovechar el pipeline de procesamiento de voz optimizado del dispositivo.

\subsection{Coherencia salida--acciones en la interfaz}
La salida del clasificador es un vector de diez logits; el \'indice del m\'aximo se mapea a la etiqueta l\'exica y de ah\'i a acciones de navegaci\'on o de control del hogar simulado (p.\,ej.\ mover foco con \texttt{up/down/left/right}, confirmar con \texttt{yes/no}, conmutar dispositivos con \texttt{on/off} e iniciar/detener rutinas con \texttt{go/stop}). Para filtrar predicciones de baja certeza y reducir activaciones falsas derivadas del desajuste acústico entre el conjunto de entrenamiento y el entorno real del dispositivo, se aplica un umbral de confianza de 0.6 sobre la probabilidad softmax m\'axima; las predicciones que no superan dicho umbral son descartadas sin despachar ninguna acci\'on. La aplicaci\'on est\'a implementada en Jetpack Compose \cite{jetpack_compose} como experiencia navegable; los comandos se generan a partir de inferencia local sobre el micr\'ofono y se despachan a un manejador de navegaci\'on/estado que traduce cada comando a cambios de foco, navegaci\'on entre pantallas y acciones sobre dispositivos o rutinas.

\begin{figure}[htbp]
\centering
\begin{subfigure}{0.48\linewidth}
  \includegraphics[width=\linewidth]{images/android_demo_home_dashboard.png}
  \caption{Home (Devices).}
\end{subfigure}\hfill
\begin{subfigure}{0.48\linewidth}
  \includegraphics[width=\linewidth]{images/android_demo_routines.png}
  \caption{Routines.}
\end{subfigure}
\caption{Aplicaci\'on Android: demo de la interfaz para dispositivos y rutinas.}
\label{fig:android_onnx}
\end{figure}

\section{Conclusiones}

Se abord\'o el reconocimiento de diez comandos de voz sobre \textit{Speech Commands} v0.02 con partici\'on oficial train/val/test y representaci\'on mel reescalada a $128 \times 128$. Doce entrenamientos (dos arquitecturas, datos crudos o aumentados, tres configuraciones de optimizador y lote) mostraron que MobileNetV2 supera de forma sostenida a LeNet-5: la mejor exactitud en prueba ($0.9633$) y F1 macro ($0.9631$) se obtuvieron con el Modelo B en datos crudos y Configuraci\'on~3 (Tabla~\ref{tab:doce_runs}). 

El Modelo A (LeNet-5) alcanz\'o su mejor desempe\~no con Configuraci\'on~1 en datos aumentados ($0.7986$ accuracy, $0.7986$ F1-macro), demostrando que la arquitectura cl\'asica requiere significativa aumentaci\'on de datos para generalizar adecuadamente. La brecha de desempe\~no (79.86\% vs 96.33\%) subraya la importancia de la profundidad de red y la capacidad expresiva para tareas ac\'usticas modernas: mientras LeNet-5 permanece viable para dispositivos altamente restringidos, MobileNetV2 proporciona robustez superior con complejidad computacional a\'un pr\'actica.

Desde el criterio m\'ovil, MobileNetV2 ofrece un mejor equilibrio entre precisi\'on y coste computacional que LeNet-5; el modelo seleccionado para despliegue debe maximizar la m\'etrica acordada en validaci\'on/prueba sujeto a restricciones de latencia, tama\~no del archivo ONNX y memoria RAM en el terminal. La trazabilidad en Weights \& Biases aporta evidencia auditable de cada corrida.

Como trabajo futuro queda cerrar el ciclo con exportaci\'on ONNX versionada, pruebas de paridad sistem\'aticas en dispositivo y sustituir la demo programada por inferencia continua sobre el micr\'ofono con el mismo contrato de preprocesamiento descrito en este informe.
Como trabajo futuro queda ampliar la evaluaci\'on en dispositivo (latencia, consumo y robustez bajo ruido) y endurecer las pruebas de paridad extremo a extremo (WAV$\to$tensor$\to$logits) sobre un conjunto m\'as grande de ejemplos, asegurando que el preprocesamiento m\'ovil permanezca bit-a-bit alineado con el contrato de entrenamiento.

\section{Referencias}

\begin{thebibliography}{99}

\bibitem{specaugment}
D.~S. Park, W.~Chan, Y.~Zhang, C.-C. Chiu, B.~Zoph, E.~D. Cubuk, and Q.~V. Le, ``SpecAugment: A Simple Data Augmentation Method for Automatic Speech Recognition,'' in \emph{Proc. INTERSPEECH}, 2019, pp.~2613--2617.

\bibitem{onnxruntime}
ONNX Runtime contributors, ``ONNX Runtime,'' 2026. [Online]. Available: \url{https://onnxruntime.ai/}. [Accessed: May 4, 2026].

\bibitem{wandb}
L.~Biewald, ``Experiment Tracking with Weights and Biases,'' Weights \& Biases, San Francisco, CA, USA, 2020. [Online]. Available: \url{https://wandb.com/}. [Accessed: May 4, 2026].

\bibitem{tensorflow_lite_android}
TensorFlow, ``TensorFlow Lite,'' 2026. [Online]. Available: \url{https://www.tensorflow.org/lite/guide}. [Accessed: May 2, 2026].

\bibitem{tensorflow_datasets_speech_commands}
TensorFlow Datasets, ``speech\_commands,'' 2026. [Online]. Available: \url{https://www.tensorflow.org/datasets/catalog/speech_commands}. [Accessed: May 2, 2026].

\bibitem{torchaudio_docs}
PyTorch, ``torchaudio,'' 2026. [Online]. Available: \url{https://docs.pytorch.org/audio/stable/index.html}. [Accessed: May 2, 2026].

\bibitem{android_mediarecorder}
Android Developers, ``MediaRecorder,'' 2026. [Online]. Available: \url{https://developer.android.com/reference/android/media/MediaRecorder}. [Accessed: May 2, 2026].

\bibitem{jetpack_compose}
Android Developers, ``Get started with Jetpack Compose,'' 2026. [Online]. Available: \url{https://developer.android.com/develop/ui/compose/documentation}. [Accessed: May 2, 2026].

\end{thebibliography}

\end{document}
